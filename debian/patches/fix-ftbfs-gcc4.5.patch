From: pquiney
Subject: Fix build with gcc-4.5
Bug-Archlinux: http://aur.archlinux.org/packages.php?ID=2793
Bug-Debian: http://bugs.debian.org/cgi-bin/bugreport.cgi?bug=622079
---
 src/main.cpp  |    2 +-
 src/mpgfile.h |    2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

--- dvbcut.orig/src/main.cpp
+++ dvbcut/src/main.cpp
@@ -171,7 +171,7 @@ main(int argc, char *argv[]) {
       return 1;
     }
 
-    index::index idx(*mpg);
+    class index idx(*mpg);
     int pics = idx.generate();
     if (pics <= 0) {
       fprintf(stderr, "%s: %s: %s\n", argv0, mpgfilename.c_str(),
--- dvbcut.orig/src/mpgfile.h
+++ dvbcut/src/mpgfile.h
@@ -49,7 +49,7 @@ protected:
   stream s[MAXAVSTREAMS];
   int videostreams, audiostreams;
   int initialoffset;
-  index::index idx;
+  class index idx;
   int pictures;
 
   mpgfile(inbuffer &b, int initial_offset);
